#!/bin/bash

shell=$(basename "$SHELL")

# comprobar si está disponible la opción reconectar al espacio de nombre del usuario
if [ -n "$(command -v reattach-to-user-namespace)" ]; then
    reattach-to-user-namespace -l "$shell"
else
    exec "$shell -l"
fi
